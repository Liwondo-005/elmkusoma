# Docker Configuration

This directory contains Docker-related configuration files.

- `Dockerfile.core` — Core service Dockerfile
- `Dockerfile.realtime` — Realtime service Dockerfile
- `Dockerfile.workers` — Workers service Dockerfile
- `Dockerfile.media` — Media service Dockerfile

Service-specific Dockerfiles are located in their respective backend directories.
